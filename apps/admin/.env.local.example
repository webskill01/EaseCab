# EaseCab Admin — local env. Copy to .env.local and adjust.
# The admin panel talks to the same API as the web app (admin auth = email+password
# against /api/v1/admin/auth, isolated admin JWT cookies — CLAUDE.md §6).
NEXT_PUBLIC_API_URL=http://localhost:4000
