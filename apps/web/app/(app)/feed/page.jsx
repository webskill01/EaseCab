import { FeedScreen } from '@/features/rides/components/FeedScreen'

/** Rides feed — the hero surface (Step 18). SSE-driven live feed + filter + sub-tabs. */
export default function FeedPage() {
  return <FeedScreen />
}
