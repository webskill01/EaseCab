import { BlockedUsersScreen } from '@/features/blocks/components/BlockedUsersScreen'

/** Blocked users (Profile → Blocked) — list + unblock. */
export default function BlockedPage() {
  return <BlockedUsersScreen />
}
