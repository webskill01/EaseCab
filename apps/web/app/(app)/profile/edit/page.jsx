import { EditProfileScreen } from '@/features/profile/components/EditProfileScreen'

/** Edit profile (#20) — dedicated route, not an inline form on /profile. */
export default function EditProfilePage() {
  return <EditProfileScreen />
}
