import { LanguageScreen } from '@/features/shell/components/LanguageScreen'

/** Language picker (§4.B) — dedicated route off the profile hub. */
export default function LanguagePage() {
  return <LanguageScreen />
}
