import { SettingsScreen } from '@/features/shell/components/SettingsScreen'

/** Settings tab (Step 21d) — notification prefs + language + support + logout. */
export default function SettingsPage() {
  return <SettingsScreen />
}
