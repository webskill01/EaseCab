# Web Features

Feature-first structure (CLAUDE.md §2). Each feature is self-contained —
components, hooks, services, tests — e.g. `features/auth/`, `features/rides/`.
Shared design-system primitives live in `components/ui/` (shadcn). The first
real feature folder lands in Step 17 (login flow).
